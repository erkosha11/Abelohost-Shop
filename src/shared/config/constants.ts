export const ROUTES = {
  HOME: '/',
  LOGIN: '/login'
} as const

export const PRODUCTS_LIMIT = 12

export const VALIDATION = {
  MIN_LENGTH: 3,
  REQUIRED_MESSAGE: 'This field is required',
  MIN_LENGTH_MESSAGE: 'Minimum 3 characters required'
} as const
